repos:
  # ──────────────────────────────────────────────────────────────────────────────
  # Hooks généraux (trailing whitespace, fin de fichier, YAML/JSON valide…)
  # ──────────────────────────────────────────────────────────────────────────────
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json

  # ──────────────────────────────────────────────────────────────────────────────
  # Black (Python) — un hook par service, pour prendre la config locale
  # ──────────────────────────────────────────────────────────────────────────────
  - repo: https://github.com/psf/black
    rev: 26.1.0
    hooks:
      - id: black
        name: Black Gateway
        language_version: python3
        files: ^backend/gateway/.*\.py$
        args:
          - --config=backend/gateway/pyproject.toml
      - id: black
        name: Black admin-service
        language_version: python3
        files: ^backend/admin-service/.*\.py$
        args:
          - --config=backend/admin-service/pyproject.toml
      - id: black
        name: Black user-service
        language_version: python3
        files: ^backend/user-service/.*\.py$
        args:
          - --config=backend/user-service/pyproject.toml
      - id: black
        name: Black scan-service
        language_version: python3
        files: ^backend/scan-service/.*\.py$
        args:
          - --config=backend/scan-service/pyproject.toml
      - id: black
        name: Black pdf-service
        language_version: python3
        files: ^backend/pdf-service/.*\.py$
        args:
          - --config=backend/pdf-service/pyproject.toml
      - id: black
        name: Black crawl-service
        language_version: python3
        files: ^backend/crawl-service/.*\.py$
        args:
          - --config=backend/crawl-service/pyproject.toml

  # ──────────────────────────────────────────────────────────────────────────────
  # isort (Python) — un hook par service, pour prendre la config locale
  # ──────────────────────────────────────────────────────────────────────────────
  - repo: https://github.com/PyCQA/isort
    rev: 8.0.1
    hooks:
      - id: isort
        name: isort Gateway
        files: ^backend/gateway/.*\.py$
        args:
          - --profile=black
      - id: isort
        name: isort admin-service
        files: ^backend/admin-service/.*\.py$
        args:
          - --profile=black
      - id: isort
        name: isort user-service
        files: ^backend/user-service/.*\.py$
        args:
          - --profile=black
      - id: isort
        name: isort scan-service
        files: ^backend/scan-service/.*\.py$
        args:
          - --profile=black
      - id: isort
        name: isort pdf-service
        files: ^backend/pdf-service/.*\.py$
        args:
          - --profile=black

  # ──────────────────────────────────────────────────────────────────────────────
  # Flake8 (Python) — un hook par service, pour prendre la config locale
  # ──────────────────────────────────────────────────────────────────────────────
  - repo: https://github.com/PyCQA/flake8
    rev: 7.3.0
    hooks:
      - id: flake8
        name: Flake8 Gateway
        files: ^backend/gateway/.*\.py$
        args:
          - --config=backend/gateway/.flake8
      - id: flake8
        name: Flake8 admin-service
        files: ^backend/admin-service/.*\.py$
        args:
          - --config=backend/admin-service/.flake8
      - id: flake8
        name: Flake8 user-service
        files: ^backend/user-service/.*\.py$
        args:
          - --config=backend/user-service/.flake8
      - id: flake8
        name: Flake8 scan-service
        files: ^backend/scan-service/.*\.py$
        args:
          - --config=backend/scan-service/.flake8
      - id: flake8
        name: Flake8 pdf-service
        files: ^backend/pdf-service/.*\.py$
        args:
          - --config=backend/pdf-service/.flake8
      - id: flake8
        name: Flake8 crawl-service
        files: ^backend/crawl-service/.*\.py$
        args:
          - --config=backend/crawl-service/.flake8

  # ──────────────────────────────────────────────────────────────────────────────
  # ESLint (TypeScript/React) pour frontend Next.js
  # ──────────────────────────────────────────────────────────────────────────────
  - repo: https://github.com/pre-commit/mirrors-eslint
    rev: v10.0.2
    hooks:
      - id: eslint
        name: Frontend Next.js ESLint
        language: node
        entry: bash -c "cd frontend && npm run lint"
        files: ^frontend/.*\.(js|jsx|ts|tsx)$
        pass_filenames: false

  # ──────────────────────────────────────────────────────────────────────────────
  # Prettier (TypeScript/JS/CSS/MD/JSON) pour frontend Next.js
  # ──────────────────────────────────────────────────────────────────────────────
  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v4.0.0-alpha.8
    hooks:
      - id: prettier
        name: Prettier Frontend Next.js
        language: node
        entry: bash -c "cd frontend && npm run format"
        files: ^frontend/.*\.(js|jsx|ts|tsx|css|json|md)$
        pass_filenames: false
